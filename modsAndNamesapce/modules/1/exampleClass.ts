// declaration file generated for if you want to share the js file so people can use it with their typescript withiout needing your typescript
// tsc exampleClass --declaration


// external file, use dts-gen by microsoft
// npm install -g dts-gen 
// then install module globally: npm install change -g 
export class SomeClass{
    constructor(message: string) {
        console.log(message);
    }
    interval : number = 1000;
    title : string = 'this title'
    method1: Function = () => {

    }
    method2: Function = () => {

    }
}